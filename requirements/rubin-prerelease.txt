# Public CUDA 13.4 prerelease PyTorch nightly pins for Rubin bring-up.
# Installed when INSTALL_RUBIN_PRERELEASE=true (see docker/Dockerfile).
# TODO(rubin-bringup): delete once Rubin APIs ship in regular PyTorch wheels
# (vllm-project/vllm#49735).
--extra-index-url https://download.pytorch.org/whl/nightly/cu134
--extra-index-url https://flashinfer.ai/whl/cu134
--extra-index-url https://flashinfer.ai/whl

torch==2.15.0.dev20260903+cu134
# The aarch64 torchvision nightly is dated one day after the torch nightly
# required by its wheel metadata, unlike the same-day x86_64 pairing.
torchvision==0.30.0.dev20260903+cu134; platform_machine == "x86_64"
torchvision==0.30.0.dev20260904+cu134; platform_machine == "aarch64"
torchaudio==2.11.0.dev20260902+cu134

# Keep the Rubin dependency set explicit because docker/Dockerfile installs it
# after the regular CUDA and KV-connector requirements.
nvidia-cutlass-dsl[cu13]==4.8.0.dev0
# Quack 0.6.4 pins CUTLASS DSL 4.6.2. This public post-release commit carries
# the tested CUTLASS DSL 4.8 compatibility work until the next Quack release.
# The adjacent CUTLASS pin supplies its cu13 extra; enabling Quack's cu13 extra
# would select Quack's public cu130 Torch index instead of this manifest's
# CUDA 13.4 nightly Torch build.
quack-kernels @ git+https://github.com/Dao-AILab/quack.git@14b0b7122e252a4df40a0b762c7ffd0f8b67a015
flashinfer-python==0.6.18.post1
flashinfer-jit-cache==0.6.18.post1+cu134
flashinfer-cubin==0.6.18.post1
cuda-python==13.4.1
cuda-bindings==13.4.1
nixl==1.4.1
